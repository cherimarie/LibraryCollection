<%= form_for @loan do |l| %>

  <%= l.hidden_field :book_id %>

  <%= l.label :user_id %>

  <%= collection_select(:loan, :user_id, User.all, :id, :name, { disabled: -> (user) { !user.good_to_borrow? }}) %>

  <%= l.submit "submit" %>

<% end %>